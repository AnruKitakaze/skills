# Implementation Checklist

- Read the local code style before editing.
- Keep the diff scoped to the task.
- Prefer existing helpers and patterns.
- Add or update focused tests where behavior changes.
- Run the narrowest useful checks first, then broader checks if shared behavior changed.
- Surface contradictions instead of guessing.
- Remove temporary debug output and throwaway files.
- Report checks run and checks not run.
